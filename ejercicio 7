#include <stdio.h>

int main() {
   int n;
   int a[10];
   double media = 0;
   puts("Ingrese 10 numeros");
   for(int i = 0; i < 10; i++){
       scanf("%d", &n);
       a[i] = n;
       media += n;
     }
    for(int i = 0; i < 10; i++){
        if(a[i] > media / 10){
            printf("%d ", a[i]);
        }
    }
    printf("\n");
    return 0;
}
